export default function parse(input: string, path: string[]): [string, CLI.BinInput | null][]
{
    input = input.trim();

    if (input === "")
        return [["", null]];

    return [["", null]];
}
